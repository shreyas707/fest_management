<%= form_for(@invite) do |f| %>
  <% if @invite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invite.errors.count, "error") %> prohibited this invite from being saved:</h2>

      <ul>
      <% @invite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <div class="ui input"><%= f.text_field :title %></div><br/><br/>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %><br/><br/>
  </div>

  <div class="field">
    <%= f.collection_select :college_department_ids, CollegeDepartment.all, :id, :college_name_with_department, {prompt: "Select Colleges"}, {class: "ui fluid search dropdown", multiple: ""} %><br/>
  </div>

  <div class="actions">
    <%= f.submit "Submit", class: "ui primary button" %><br/><br/>
  </div>
<% end %>
