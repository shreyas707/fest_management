<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @invite.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @invite.body %>
</p>

<p>
	<strong>Invites:</strong><br/>
	<% @invite.college_department_invites.each do |cdi| %>
		<%= cdi.college_department.college.name %>
		<%= cdi.college_department.department.name %>
	<% end %>
</p>

<%= link_to 'Edit', edit_invite_path(@invite) %> |
<%= link_to 'Back', invites_path %>
