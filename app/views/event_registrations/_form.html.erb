<%= form_for(@event_registration) do |f| %>
  <% if @event_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_registration.errors.count, "error") %> prohibited this event_registration from being saved:</h2>

      <ul>
      <% @event_registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <div class="ui input"><%= f.text_field :name %></div><br/><br/>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <div class="ui input"><%= f.text_field :email %></div><br/><br/>
  </div>
  <div class="field">
    <%= f.label :college %><br>
    <div class="ui input"><%= f.text_field :college %></div><br/><br/>
  </div>
  <div class="field">
    <%= f.label :department %><br>
    <div class="ui input"><%= f.text_field :department %></div><br/><br/>
  </div>
  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.collection_select :event_id, Event.all, :id, :full_event_name, {prompt: "Select Event"}, {class: "ui search dropdown"} %>
  </div><br/>
  <div class="actions">
    <%= f.submit "Submit", class: "ui primary button" %><br/><br/>
  </div>
<% end %>
